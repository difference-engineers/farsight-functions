#!/usr/bin/env ruby

File.read("SERVICES").split("\n").each do |name|
  puts "Checking for errors in #{name}..."
  exit unless system("bin/local-lint #{name}")
  exit unless system("bin/local-typecheck #{name}")
end