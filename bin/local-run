#!/bin/bash

set -e

service=$1

echo "function(request: MOCK_REQUEST, database: DATABASE, logger: Async.logger)" | bin/local $service bundle exec ruby \
  -r "./boot" \
  -r "./configuration" \
  -r "./application" \
  -r "./database" \
  -r "./function" \
  -r "./mocks"
