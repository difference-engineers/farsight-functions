# The Docker image to run your workspace in. Defaults to gitpod/workspace-full
image: gitpod/workspace-postgres
tasks:
  - command: "bin/functions-automatic-create && bin/local-setup"
ports:
  - port: 8080
